{
  "name": "philip-builds-studio",
  "version": "1.0.0",
  "private": true,
  "description": "Static front-end studio site with an optional, provider-agnostic AI chat function (api/chat.js). GitHub Pages serves the frontend; the function is dormant until deployed with an env key.",
  "type": "module",
  "scripts": {
    "serve": "python3 -m http.server 8080",
    "check:api": "node --check api/chat.js"
  },
  "engines": {
    "node": ">=18"
  }
}
